// config/antd-theme-config.ts
import { theme } from "antd";

const { defaultAlgorithm } = theme;

const antdThemeConfig = {
  algorithm: defaultAlgorithm,
  token: {
    colorPrimary: "#1DA57A",
  },
};

export default antdThemeConfig;
